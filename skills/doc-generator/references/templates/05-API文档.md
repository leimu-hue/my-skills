# 接口设计文档

**项目名称**：XXX项目 + 功能名称

## 一、接口清单

[要求：列出本次需求涉及的所有接口，至少包含模块、类型、URI、协议、用途与场景描述]

| 模块 | 类型 | 接口 | 协议 | 用途与场景描述 |
|------|------|------|------|---------------|
| [模块名称] | [类型: POST、GET 等] | [接口地址] | HTTPS | [含义] |

## 二、接口定义

[要求：按“每个接口一个小节”填写，不可省略请求参数、返回参数、响应码说明、调用示例]
[参数格式规则：拼接在 URI 上的参数必须写为表格；放在请求体中的参数必须写为 JSON 格式]
[请求体规则：若有请求体，需提供 JSON 示例与“请求体字段说明表（字段/类型/必填/说明）”；若无请求体，直接写“无”]

### 2.1 接口定义模板（复制本小节）

**接口名称**：<简述接口功能>

**描述**：<详细描述业务场景和用途>

**URI**：/xxx/xxx

**协议**：HTTPS

**请求方法**：GET/POST/PUT/DELETE

**请求参数（Query String）**：

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| param1 | string | 是 | 参数说明 | value1 |

**请求体（Body）**：若有，请按以下格式提供；若无，写“无”。

```json
{
	"field1": "示例值"
}
```

**请求体字段说明**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|----------|------|
| field1 | string | 是 | 字段用途 |

**返回参数**：

| 属性 | 类型 | 说明 |
|------|------|------|
| success | boolean | 成功标志 |
| code | long | 响应码 |
| msg | string | 提示信息 |

**响应码说明**：

| 响应码 | 说明 | 处理建议 |
|--------|------|----------|
| 0 | 成功 | - |
| -1 | 错误描述 | 处理方法 |

**调用示例**：

```text
/xxx/xxx?param=value
```

**返回示例**：

```json
{
	"success": true,
	"code": 0,
	"msg": ""
}
```