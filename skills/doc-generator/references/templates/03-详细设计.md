# 03-详细设计 模板

```markdown
# 详细设计

**项目名称**：XXX项目

## 变更日志

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|----------|
| <动态生成> | YYYY-MM-DD | <作者> | 初始版本 |

---

## 一、核心类详细设计

> 要求：每个核心类独立一小节，覆盖类的职责、关键方法算法流程、分支逻辑。

### 1.1 XXXService 详细设计

**职责**：[一句话描述该类的核心职责]

#### 方法：change(cmd)

**输入**：ChangeCommand（包含变更类型、目标ID、变更参数）
**输出**：ChangeResult（包含变更ID、状态、是否复用已有变更）
**异常**：ValidationException（参数校验失败）、ConcurrencyException（并发冲突）

**算法流程**：

##### 初始化阶段
1. 接收 ChangeCommand，提取 `type`、`targetId`、`params` 三个核心字段
2. 调用 `validateCommand(cmd)` 校验参数合法性：
   - `type` 必须在允许的变更类型枚举中
   - `targetId` 非空且格式符合业务规则
   - `params` 中的必填字段均已提供
3. 若校验失败 → 抛出 `ValidationException`，附带字段级错误详情

##### 核心逻辑
1. 调用 `repository.findPendingChange(type, targetId)` 查询是否存在进行中的变更单
2. **分支1 — 已存在进行中变更单**：
   - 返回 `ChangeResult(existed=true, changeId=existingChange.id)`
   - 不创建新记录，复用已有变更
3. **分支2 — 可新建变更**：
   - 从 `repository` 读取当前实体快照，记录变更前版本
   - 构造新 `Change` 实体，状态设为 `DRAFT`
   - 调用 `repository.save(change)` 持久化
   - 返回 `ChangeResult(existed=false, changeId=newChange.id)`

##### 异常处理
| 异常类型 | 触发条件 | 处理方式 |
|----------|----------|----------|
| ValidationException | 参数校验不通过 | 返回 400 + 字段错误详情 |
| ConcurrencyException | 同目标并发创建变更 | 返回 409 + 提示稍后重试 |
| DataAccessException | 数据库读写异常 | 返回 500 + 错误追踪ID |

### 1.2 XXXController 详细设计

**职责**：接收 HTTP 请求，参数适配，调用应用层服务，返回统一响应格式。

#### 方法：change(HttpServletRequest)

**输入**：HTTP 请求（含 Query String / Body）
**输出**：`Result<ChangeResult>`（统一响应包装）
**异常**：由全局异常处理器统一捕获

**算法流程**：
1. 从请求中提取参数，构造 `ChangeCommand`
2. 获取当前用户信息（从 Token/Session 中解析）
3. 调用 `applicationService.change(cmd)` 执行业务逻辑
4. 将 `ChangeResult` 包装为 `Result.success(data)` 返回
5. 若捕获业务异常 → 转换为对应的 `Result.error(code, msg)` 返回

---

## 二、关键逻辑分支详解

> 要求：对复杂分支单独展开说明，包含条件判断伪代码和处理逻辑。

### 2.1 变更单复用 vs 新建分支

```
IF repository.findPendingChange(type, targetId) != null THEN
    // 复用路径：不产生新变更单
    RETURN ChangeResult(existed=true, changeId=existing.id)
ELSE
    // 新建路径：创建变更单并记录变更前版本
    snapshot = repository.findCurrentState(targetId)
    change = Change.create(type, targetId, params, snapshot)
    repository.save(change)
    RETURN ChangeResult(existed=false, changeId=change.id)
END IF
```

**注意事项**：
- 复用路径不修改任何数据，仅返回已有变更单ID
- 新建路径必须在保存变更单的同时记录变更前版本，用于回滚
- 并发场景下需加分布式锁或依赖数据库唯一约束

### 2.2 并发校验分支

```
// 在 repository.save(change) 中通过数据库约束保证
// 或通过分布式锁：lock_key = "change:{type}:{targetId}"
IF acquireLock(type, targetId, timeout=3s) FAILS THEN
    THROW ConcurrencyException("另一请求正在创建变更，请稍后重试")
END IF
```

---

## 三、算法说明

### 3.1 变更前版本快照算法

**用途**：在创建变更前记录当前数据状态，支持回滚和差异对比。

**伪代码**：
```
FUNCTION captureSnapshot(targetId):
    entity = repository.findById(targetId)
    IF entity == null THEN
        THROW EntityNotFoundException("目标实体不存在")
    END IF
    snapshot = Snapshot.create(
        targetId = targetId,
        entityType = entity.type,
        data = serializeToJson(entity),
        version = entity.version
    )
    RETURN snapshot
END FUNCTION
```

**时间复杂度**：O(1) — 单条记录读取 + JSON 序列化
**空间复杂度**：O(n) — n 为实体 JSON 序列化后的大小
```
